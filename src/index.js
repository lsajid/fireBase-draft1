import { initializeApp } from "firebase/app";
// import { getAuth } from "firebase/auth"


//firebase App instance this instance is how firebaseSDK knows how to connect to specific firebase backend
const firebaseApp = initializeApp({

});